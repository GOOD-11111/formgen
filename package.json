{
  "name": "formgen",
  "version": "0.1.0",
  "private": true,
  "description": "低代码与智能表单生成引擎：自然语言需求 → 表单/考卷 Schema → 运行时动态渲染 → 采集、存储与评阅",
  "type": "module",
  "engines": {
    "node": ">=20.6"
  },
  "bin": {
    "formgen": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test --test-isolation=none \"test/**/*.test.js\"",
    "start": "node src/cli.js serve",
    "check": "npm test && node src/cli.js doctor"
  },
  "dependencies": {},
  "devDependencies": {}
}
