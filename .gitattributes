# 统一换行符：仓库内一律 LF，避免 Windows 上 core.autocrlf 造成整文件 diff 噪音。
* text=auto eol=lf

# 二进制资源不要做任何文本处理
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff2 binary
# 压缩流尤其不能：被当成文本做换行归一会直接损坏内容
*.zst binary
*.gz binary
*.zip binary
